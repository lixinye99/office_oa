<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.officeoa.mapper.UserExtraMapper">
    <select id="getUserRolesByUserId" resultType="com.example.officeoa.model.Role">
        select * from role r,user_role ur where r.rid = ur.rid and ur.uid = #{userId}
    </select>

    <select id="getManagerUserList" resultType="String">
        select username from userinfo u,user_role ur,role r where ur.uid = u.uid and ur.rid = r.rid and r.roleNameZh = "部门管理员"
    </select>
</mapper>